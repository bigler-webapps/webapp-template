# WORK_ORDERS.md — webapp-template

Work-order register for this repo. Lightweight directory (not the full orders):
one row per WO with its implementation status. Convention, schema, and maintenance
rules are defined centrally in `webapps/AGENTS.md` → "Work-Order Register".

## Workstream prefixes

| Prefix | Workstream |
|---|---|
| `TPL-*` | Preventive template baseline work inherited by newly scaffolded applications |

Introduce a new prefix when none fits and add it here. New WOs always get a
prefixed ID; never reuse a bare flat number across workstreams.

## Register

| ID | Titel | Beschreibung | Datum | Status | Commit(s) | Notiz |
|---|---|---|---|---|---|---|
| INF-38 | ucm catch-up, the mechanical half — pin to `2.41.3` plus the i18n line | `frontend/package.json` `@micha.bigler/ui-core-micha` `2.31.1` → `2.41.3` plus `frontend/i18n/index.js` `authTranslations` → `uiCoreTranslations`, in one commit, plus a namespace-registration regression test. Canonical order `webapp-management/work-orders/INF-38.md`; this repo's slice: [`work-orders/INF-38.md`](work-orders/INF-38.md) | 2026-08-19 | done | (pending) | Tier 3 (shared-core dependency). Codex recorded `unavailable` for today — implemented directly, both reviewers mandatory (already required by WO Part C). Matters most of the ten Group-A apps: this is the scaffold every new app is cut from, so leaving it on `authTranslations`-only would re-create the gap in apps that don't exist yet. Full frontend suite (2 files, 6 tests) green. `reviewer` (Sonnet): no findings. `ui_reviewer` (Sonnet): no findings — specifically checked for a dead `.MuiTabs-root`-style selector (per the defect found in survey_app's dispatch); none present. Live browser check not run (no operator available); substituted with the regression test per this WO's own Part C. `createAppTheme` adoption stays explicitly out of scope (`INF-39`'s `can` tranche). |
| INF-37 | Django 6.1 + dcm 2.43.0 | Two-line pin bump. Canonical order `webapp-management/work-orders/INF-37.md`; this repo's slice: [`work-orders/INF-37.md`](work-orders/INF-37.md) | 2026-08-19 | done | `a106e55` | Tier 3. Landed without a local register row at the time — backfilled here. This app has no deployment/database (matrix S2: "n/a"), and is one of OPS-4's two `pre-existing-red` apps (20 failed/2 passed at both pins — an unrelated, already-known `test_settings.py` placeholder bug, not caused by this bump). Per the WO's own instruction: record and proceed, don't let a red baseline block the bump, don't claim a green one. Matters out of proportion since every new app scaffolds from this repo's current state. |
| TPL-1 | Bring the template frontend onto the shared theme baseline and current `ui-core-micha` | Adopts `createAppTheme` and its hard completeness assertion, pins the current shared UI package (`2.31.1`), and fixes light-scheme and safe-area handling for every future scaffold. Full spec: [work-orders/TPL-1.md](work-orders/TPL-1.md). | 2026-08-10 | done | bd61e45 | DS-17. One-step dependency bump is deliberate: the assessed intervening releases are feature-only apart from the mail switch, and the template has no onboarding or notification integration that can inherit that default (confirmed by grep — no `Onboarding`/`Notifications`/`browserPush` usage anywhere in the template). The provisional `#FF00FF` accent is intentionally conspicuous and must be replaced by each new app. **Independent `reviewer` (Sonnet) + `ui_reviewer` (Haiku) ran concurrently.** `ui_reviewer`: no findings. `reviewer` found **R1 (P3)** — this row's status used `in review` (space) instead of the schema's hyphenated `in-review`/`done` enum; superseded by this row going straight to `done`. Two things the Orchestrator itself fixed (not reviewer findings, found via the template's own live test run — the mandatory verification step, not a review): (1) Codex bumped `package.json`'s pin but never ran `pnpm install`, leaving `pnpm-lock.yaml` stale at `2.5.0` — installed and re-locked at `2.31.1`. (2) `App.test.jsx`'s mount test asserted `getByRole('heading', {level:1})`, but `Home` renders two `h4`s and no `h1` — a pre-existing heading-level choice unrelated to this WO's scope; the assertion was loosened to `getAllByRole('heading').length > 0` rather than forcing an `h1` into the page. Re-verified: 4/4 scoped vitest tests green, `pnpm lint` 0 errors (18 pre-existing, unrelated warnings). **Two-width rendered side-by-side (DS-1 gate)** run at 375px/1280px against the template's own dev server before commit — the magenta placeholder is unmistakable at both widths. **The notched-viewport emulation the WO required was NOT possible**: the browser pane cannot produce non-zero `env(safe-area-inset-*)` values (that needs real hardware or a DevTools override). A computed-style verification was substituted, confirming the `env()` rule resolves in the `body` rule — which proves the CSS is wired, not that content stays clear of a notch. **The rendered safe-area behaviour therefore remains unverified**, and every scaffolded app inherits it. |
| TPL-3 | Stop scaffolding new apps onto a PostGIS database | `docker-compose.yml:86` pins `postgis/postgis:14-3.1`, so every app scaffolded from this template inherits a PostGIS runtime database. Measured across the estate on staging 2026-08-17: app-owned geometry columns exist in hram alone, and this template declares no `django.contrib.gis`, no postgis DB ENGINE and no geometry field anywhere — the image is pure scaffolding inertia that then propagates the `postgis`, `postgis_topology` and `postgis_tiger_geocoder` extensions into each new app's database at initdb. Change the one line to `postgres:14`. | 2026-08-17 | done | e3875eb | Tier 1 (config toggle in a template with no deployment behind it) — therefore a register row only, no `work-orders/TPL-3.md`, per `AGENTS.md` → Tiering. Tests: none; verification is this repo's own CI staying green. `postgres:14`, deliberately **not** 15/16 — a major bump carries an incompatible PGDATA and is a separate change. **Scoped to the compose line only.** `backend/Dockerfile:38-41` (`gdal-bin`, `libgdal-dev`, `libproj-dev`, `libgeos-dev`) and `:62-63` (the `GDAL_LIBRARY_PATH` export, which has no consumer — Django reads the Django *setting*, not the environment variable) are left to the build-toolchain workstream, which rewrites the same apt block; two orders must not edit the same lines. Companion to `workflow-templates` CI-16, which retires the same image as the CI default, but independent of it — this one has no ordering constraint. **Landed 2026-08-17, Tier-1 self-check** (no independent reviewer, per the Tier-1 gate): the diff is the single `image:` line and nothing else; `git grep` confirms `postgis` / `contrib.gis` now appear nowhere in the tracked tree outside this register; the service's env vars, the `postgres_data` volume and the `backup.*` labels are untouched, so no scaffolded app changes anything but its base image. Directly-affected tests: none — a compose image tag has no assertable application behaviour, and the template ships no test that reads it. Trunk here is `main` (this repo has no `develop`, despite the boilerplate line in its own `CLAUDE.md`). |
| TPL-2 | Stop shipping an unenforced frontend suite to every new app | `ci.yml` ships `run-frontend-tests: false`, so every scaffolded app inherits an unenforced frontend suite and the flag never gets revisited. Flips the default to `true` and replaces the inherited "~30 s" comment with a runtime measured in a real app. Full spec: [work-orders/TPL-2.md](work-orders/TPL-2.md). | 2026-08-12 | done | c566e54 | Tier 3 (CI). Codex known-unavailable for 2026-08-12 (`.claude/codex-status.md`), skipped straight to direct Claude implementation — authorship flip, independent `reviewer` (Sonnet) ran and returned no findings (diff matches scope exactly: line 19 flag flip + comment rewrite, nothing else touched). **Unblocked 2026-08-12** — `cockpit/UI-12` landed and its CI run is green: 12 files, 92 tests, **47.68 s** (run 31590500379), both UI-10's and UI-11's hard assertions among them. Two corrections that followed: the inherited "~30 s" comment was in the right ballpark after all (an earlier draft called it an order of magnitude wrong — retracted, that came from broken local runs), and **the flag does NOT give per-push enforcement** — `ci.yml` triggers only on `pull_request`/`workflow_dispatch`, and since these apps commit straight to the trunk with the promotion as the only PR, the suite runs on the promotion PR and on dispatch. Weaker than "blocks a commit", and exactly what AGENTS.md designs for: the narrow set is the local gate, the full suite is the promotion gate's job. Same propagation mechanism `TPL-1` fixed for the `#468AB2` accent: measured 2026-08-12, the flag is `true` in hram/jg-ferien/survey_app/reimbursements/innoservice (five apps that each opted in by hand) and `false` in cockpit/spesix/fitness-monitor **and here** — which is why FM-12's `assertThemeComplete`, FM-14's raw-key test and cockpit's UI-10/UI-11 assertions all sit unenforced. **Local measurements are explicitly NOT evidence and the WO says so:** three runs on 2026-08-12 contradicted each other (cockpit 2-of-11 with nine killed workers under concurrent load; the same single file first 8-green then erroring with all sub-timings 0ms; **this template's one test file crashing its worker on an idle machine** with `[vitest-pool]: Worker forks emitted error`), while UI-11's landing reported cockpit at 92/92 green. The failure is intermittent and environmental — plausibly vitest-4-forks / jsdom-29 / Node-24 / Windows — and none of it is a basis for touching this template's test setup, which is explicitly out of scope. |
| INF-27 | Strip the build toolchain and WeasyPrint cluster from the backend image | `backend/Dockerfile` carried a C toolchain, `-dev` headers and the unused WeasyPrint runtime cluster (cairo/pango/gdk-pixbuf) in its runtime layer, plus a dead `GDAL_LIBRARY_PATH` export. Removed all sixteen packages + the dead block; `libraqm0` and the fonts stay (measured tradeoff), so every new app scaffolded from this template inherits the lean image. Cross-repo WO, canonical Envelope + full rationale in `webapp-management/work-orders/INF-27.md`; this repo's slice: [`work-orders/INF-27.md`](work-orders/INF-27.md) | 2026-08-17 | done | (pending push) | Tier 2, cross-repo with spesix/webshop-guenter/fitness-monitor/hpc-bridge/cockpit (mutually independent, no ordering constraint). This repo has no `develop`, trunk is `main`. Implemented via Codex: `docker build --no-cache --target backend_test` exit 0, import smoke test exit 0. Independent `reviewer` (Haiku): no findings — confirmed exactly the 16 named packages removed, required packages untouched, GDAL block fully removed with no orphaned reference, requirements.txt confirmed no bare psycopg2/weasyprint/cairo consumer, no template scaffolding disturbed near the changed lines. |
